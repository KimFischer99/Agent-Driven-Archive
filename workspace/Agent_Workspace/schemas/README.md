# Schemas

Store reusable frontmatter, SQLite, knowledge graph, and export schemas here.

## What goes here

- Frontmatter field specifications per domain
- SQLite table schemas
- Knowledge graph relation type definitions
- Export / manifest JSON schemas
- Validation rules

Keep schemas versioned and date-stamped. When a schema changes, document the change in `activity_log.md`.
